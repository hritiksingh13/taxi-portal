services:
  # ── Backend API ──────────────────────────────────────────────────────────────
  - type: web
    name: naina-tour-backend
    runtime: node
    region: singapore
    plan: free
    rootDir: apps/backend
    buildCommand: npm install && npx prisma generate && npm run build
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 4000
      - key: DATABASE_URL
        fromDatabase:
          name: naina-tour-db
          property: connectionString
      - key: CLIENT_URL
        value: https://naina-tour-frontend.onrender.com
      - key: CUSTOMER_PORTAL_URL
        value: https://naina-tour-customer-portal.onrender.com
      - key: GMAIL_USER
        sync: false
      - key: GMAIL_APP_PASSWORD
        sync: false

  # ── Frontend Manager Portal ──────────────────────────────────────────────────
  - type: web
    name: naina-tour-frontend
    runtime: static
    rootDir: apps/frontend
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://naina-tour-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

  # ── Customer Portal ──────────────────────────────────────────────────────────
  - type: web
    name: naina-tour-customer-portal
    runtime: static
    rootDir: apps/customer-portal
    buildCommand: npm install && npm run build
    staticPublishPath: dist
    envVars:
      - key: VITE_API_URL
        value: https://naina-tour-backend.onrender.com
    routes:
      - type: rewrite
        source: /*
        destination: /index.html

databases:
  - name: naina-tour-db
    region: singapore
    plan: free
    databaseName: naina_tour_db
    user: naina_tour
